# ---- Secrets -------------------------------------------------------
# Never commit real credentials. Copy .env.example to .env and put the
# real MongoDB / Cloudinary values there instead.
.env
.env.local
.env.*.local

# ---- Node ----------------------------------------------------------
node_modules/
npm-debug.log*

# ---- Large archives ------------------------------------------------
# These are the two files GitHub rejected on the last push (174MB and
# 190MB — the limit is 100MB per file). Zipping the site into the repo
# is no longer needed now that photos live in Cloudinary.
/assets.zip
/admin.zip
assets/images/completed-projects.zip

# ---- macOS ---------------------------------------------------------
.DS_Store
Icon?
